import { createAction } from 'redux-act'

export const toggle = createAction('OFF_CANVAS_TOGGLE')
export const active = createAction('OFF_CANVAS_ACTIVE')
export const inactive = createAction('OFF_CANVAS_INACTIVE')
